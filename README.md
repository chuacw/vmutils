Introduction
============
This is a package to help start a VMware VM.

NewVM requires controller, which is the full path to the vmrun executable, and vmx the full path to the vmx of the VM being started.

- Start starts the VM
- Stop stops the VM

See vmutils_test for an example of code that uses this.

